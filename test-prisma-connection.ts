const { PrismaClient } = require('@prisma/client'); const { PrismaPg } = require('@prisma/adapter-pg'); const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgresql://postgres.qewtmejekyklhrlgwdng:Diego-QR%40app@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true' }); const adapter = new PrismaPg(pool); const prisma = new PrismaClient({ adapter }); prisma.invitation.findMany().then(console.log).catch(console.error).finally(() => process.exit(0));
